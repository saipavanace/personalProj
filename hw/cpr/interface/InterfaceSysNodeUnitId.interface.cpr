{
  "name": "InterfaceSysNodeUnitId",
  "interface": [
    {
      "name": "InterfaceSysNodeUnitId",
      "signals": [
        {
          "name": "n_unit_id",
          "type": "logic",
          "dir": "m_out",
          "excludable": false,
          "width": "(params.wNUnitIdV === undefined) ? (Array.isArray(params.wNUnitId) ? params.wNUnitId.reduce((a, b) => a + b, 0) : params.wNUnitId) : params.wNUnitIdV.reduce((a, b) => a + b, 0)"
        }
      ]
    }
  ]
}
