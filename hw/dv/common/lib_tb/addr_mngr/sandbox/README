Replace the files in this directory with the latest address manager files generated and run the below commands

Compile command:
vcs -full64 -sverilog -ntb_opts uvm-1.2 +incdir+$UVM_HOME/src +incdir+. mem_agent_pkg.sv smoke_tb.sv -debug_access+r+w-memcbk -debug_region+cell -l sim.log -o simv
 
Simulate command:
./simv +ntb_random_seed=5